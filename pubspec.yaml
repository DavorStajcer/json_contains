name: json_contains
description: A library for checking if a JSON object contains another JSON object.
version: 1.0.1
repository: https://github.com/DavorStajcer/json_contains

environment:
  sdk: ^3.0.0

dependencies:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
